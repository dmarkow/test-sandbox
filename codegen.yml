overwrite: true
schema: schema.graphql
documents: document.graphql
generates:
  types.ts:
    plugins:
      - add: "/* eslint-disable */\n"
      - typescript
      - typescript-operations
config:
  nonOptionalTypename: true